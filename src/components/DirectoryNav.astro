---
type NavItem = {
  label: string;
  href: string;
  key: string;
};

type Props = {
  active: "home" | "projects" | "posts" | "readings" | "people";
  label?: string;
};

const { active, label = "Directory navigation" } = Astro.props;

const items: NavItem[] = [
  { label: "Home", href: "/", key: "home" },
  { label: "Projects", href: "/projects", key: "projects" },
  { label: "Readings", href: "/readings", key: "readings" },
  { label: "People", href: "/people", key: "people" },
  { label: "Posts", href: "/posts", key: "posts" },
];

---

<aside class="sidebar" data-sidebar aria-label={label}>
  <nav class="sidebar-nav">
    {
      items.map((item) => (
        <a
          class:list={["sidebar-link", { "is-active": item.key === active }]}
          href={item.href}
          aria-current={item.key === active ? "page" : undefined}
        >
          {item.label}
        </a>
      ))
    }
  </nav>
</aside>
