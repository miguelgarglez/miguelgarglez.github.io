<section id="skills" class="skills">
  <div class="container">
    <h2 class="section-title">Technical Skills</h2>

    <ul class="skills-disclosure" role="list">
      <li class="skill-card is-active" tabindex="0">
        <article>
          <div class="skill-card-header">
            <h3 class="skill-title">UI PLATFORM & FRONTEND</h3>
          </div>
          <div class="skill-details">
            <div class="skill-details-inner">
              <ul class="skill-list" role="list">
                <li class="skill-list-item">
                  <span class="skill-name">React & TypeScript</span>
                  <span class="skill-description">
                    Component libraries at scale
                  </span>
                </li>
                <li class="skill-list-item">
                  <span class="skill-name">Design Systems</span>
                  <span class="skill-description">
                    Tokens, components, and UI governance
                  </span>
                </li>
                <li class="skill-list-item">
                  <span class="skill-name">Accessibility</span>
                  <span class="skill-description">
                    WCAG compliance and inclusive patterns
                  </span>
                </li>
                <li class="skill-list-item">
                  <span class="skill-name">Performance</span>
                  <span class="skill-description">
                    UI quality, profiling, and optimization
                  </span>
                </li>
              </ul>
            </div>
          </div>
        </article>
      </li>

      <li class="skill-card" tabindex="0">
        <article>
          <div class="skill-card-header">
            <h3 class="skill-title">BACKEND & DATA</h3>
          </div>
          <div class="skill-details">
            <div class="skill-details-inner">
              <ul class="skill-list" role="list">
                <li class="skill-list-item">
                  <span class="skill-name">Python</span>
                  <span class="skill-description">
                    Flask, Django, Pandas, NumPy, Matplotlib
                  </span>
                </li>
                <li class="skill-list-item">
                  <span class="skill-name">Databases</span>
                  <span class="skill-description">
                    SQL (PostgreSQL, SQLite) and NoSQL (MongoDB)
                  </span>
                </li>
                <li class="skill-list-item">
                  <span class="skill-name">Node.js</span>
                  <span class="skill-description">
                    MERN stack development experience
                  </span>
                </li>
              </ul>
            </div>
          </div>
        </article>
      </li>

      <li class="skill-card" tabindex="0">
        <article>
          <div class="skill-card-header">
            <h3 class="skill-title">MOBILE & CROSS-PLATFORM</h3>
          </div>
          <div class="skill-details">
            <div class="skill-details-inner">
              <ul class="skill-list" role="list">
                <li class="skill-list-item">
                  <span class="skill-name">Flutter</span>
                  <span class="skill-description">
                    Spotify API client development
                  </span>
                </li>
                <li class="skill-list-item">
                  <span class="skill-name">React Native</span>
                  <span class="skill-description">
                    Mobile app development experience
                  </span>
                </li>
                <li class="skill-list-item">
                  <span class="skill-name">Mobile UI</span>
                  <span class="skill-description">
                    Cross-platform UX and interface patterns
                  </span>
                </li>
              </ul>
            </div>
          </div>
        </article>
      </li>

      <li class="skill-card" tabindex="0">
        <article>
          <div class="skill-card-header">
            <h3 class="skill-title">ENGINEERING TOOLS & AI</h3>
          </div>
          <div class="skill-details">
            <div class="skill-details-inner">
              <ul class="skill-list" role="list">
                <li class="skill-list-item">
                  <span class="skill-name">AI-assisted development</span>
                  <span class="skill-description">
                    GitHub Copilot, MCP servers, context engineering
                  </span>
                </li>
                <li class="skill-list-item">
                  <span class="skill-name">CI/CD</span>
                  <span class="skill-description">
                    GitHub Actions, Microsoft Azure pipelines
                  </span>
                </li>
                <li class="skill-list-item">
                  <span class="skill-name">Cloud & APIs</span>
                  <span class="skill-description">
                    AWS, Vercel, Cloudflare Workers, Docker, GraphQL
                  </span>
                </li>
              </ul>
            </div>
          </div>
        </article>
      </li>
    </ul>

    <div class="philosophy">
      <div class="philosophy-content">
        <h3>Development Philosophy</h3>
        <p>
          "Strong knowledge fundamentals are the key. Technologies and
          frameworks come and go, they are tools that help engineers fix
          business problems."
        </p>
      </div>
    </div>
  </div>
</section>

<script>
  import gsap from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  document.addEventListener('DOMContentLoaded', () => {
    const cards = Array.from(document.querySelectorAll('#skills .skill-card'));
    const supportsHover = window.matchMedia('(hover: hover) and (pointer: fine)')
      .matches;

    const setActiveCard = (card) => {
      cards.forEach((item) => item.classList.remove('is-active'));
      card.classList.add('is-active');
    };

    cards.forEach((card) => {
      card.addEventListener('focusin', () => setActiveCard(card));

      if (supportsHover) {
        card.addEventListener('mouseenter', () => setActiveCard(card));
      } else {
        card.addEventListener('click', () => setActiveCard(card));
      }
    });


    // Establecer estado inicial para las animaciones
    gsap.set('#skills .section-title', { y: 50, opacity: 0 });
    gsap.set('#skills .skill-card', { y: 60, opacity: 0 });
    gsap.set('#skills .philosophy', { scale: 0.9, opacity: 0 });

    // Animación del título
    gsap.to('#skills .section-title', {
      y: 0,
      opacity: 1,
      duration: 1,
      ease: 'power2.out',
      scrollTrigger: {
        trigger: '#skills',
        start: 'top 70%',
        end: 'bottom 70%',
        toggleActions: 'play none none reverse',
      },
    });

    // Animación de las tarjetas
    gsap.to('#skills .skill-card', {
      y: 0,
      opacity: 1,
      duration: 0.8,
      stagger: 0.15,
      ease: 'power2.out',
      scrollTrigger: {
        trigger: '#skills .skills-disclosure',
        start: 'top 80%',
        end: 'bottom 80%',
        toggleActions: 'play none none reverse',
      },
    });

    // Animación de la filosofía
    gsap.to('#skills .philosophy', {
      scale: 1,
      opacity: 1,
      duration: 1,
      ease: 'power2.out',
      scrollTrigger: {
        trigger: '#skills .philosophy',
        start: 'top 80%',
        end: 'bottom 80%',
        toggleActions: 'play none none reverse',
      },
    });
  });
</script>

<style>
  .skills {
    padding: 5rem 2rem;
    background: var(--bg-light);
    color: var(--text);
    overflow: hidden;
    position: relative;
    /* Adjust if new skills are added so details don't clip in desktop. */
    --skill-card-height: 400px;
  }

  .skills::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(
        circle at 15% 20%,
        rgba(255, 180, 80, 0.08),
        transparent 55%
      ),
      radial-gradient(
        circle at 85% 15%,
        rgba(140, 220, 255, 0.06),
        transparent 50%
      );
    pointer-events: none;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    position: relative;
  }

  .section-title {
    font-size: clamp(2rem, 4vw, 3rem);
    text-align: center;
    margin-bottom: 2rem;
    color: var(--text);
    font-weight: 700;
  }

  .skills-disclosure {
    list-style: none;
    padding: 0;
    margin: 0 0 4rem;
    display: flex;
    gap: 1.5rem;
    align-items: stretch;
    flex-wrap: nowrap;
    width: 100%;
  }

  .skill-card {
    background: var(--bg);
    border-radius: 22px;
    border: 1px solid var(--border-muted);
    padding: 1.75rem 1.6rem;
    position: relative;
    overflow: hidden;
    box-sizing: border-box;
    transition:
      transform 0.35s ease,
      border-color 0.35s ease,
      box-shadow 0.35s ease,
      flex 0.5s ease;
    cursor: pointer;
    flex: 1 1 0;
    min-width: 220px;
    height: var(--skill-card-height);
  }

  .skill-card::after {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 22px;
    box-shadow: 0 0 0 rgba(0, 0, 0, 0);
    transition: box-shadow 0.35s ease;
    pointer-events: none;
  }

  .skill-card:hover,
  .skill-card:focus-within,
  .skill-card.is-active {
    transform: translateY(-4px);
    border-color: rgba(255, 180, 80, 0.65);
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.25);
    flex: 2.5 1 0;
  }

  .skill-card:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 3px;
  }

  .skill-card article {
    display: flex;
    gap: 0;
    height: 100%;
    align-items: flex-start;
  }

  .skill-card-header {
    display: flex;
    flex-direction: row;
    align-items: flex-start;
    gap: 0.8rem;
    flex: 0 0 auto;
    height: 100%;
  }

  .skill-title {
    font-size: 1.6rem;
    font-weight: 700;
    color: var(--primary);
    margin: 0;
    writing-mode: vertical-rl;
    text-orientation: mixed;
    letter-spacing: 0.08em;
    line-height: 1.1;
    height: auto;
    display: inline-flex;
    align-items: flex-start;
  }

  .skill-details {
    overflow: hidden;
    flex: 0 0 auto;
    max-width: 0;
    opacity: 0;
    transform: translateX(-8px);
    margin-left: 0;
    transition:
      max-width 0.45s ease,
      opacity 0.35s ease,
      transform 0.35s ease,
      margin-left 0.35s ease;
  }

  .skill-details-inner {
    padding-top: 0;
    opacity: 0;
    transform: translateY(-8px);
    transition:
      opacity 0.25s ease,
      transform 0.25s ease;
    transition-delay: 0s;
  }

  .skill-card:hover .skill-details-inner,
  .skill-card:focus-within .skill-details-inner,
  .skill-card.is-active .skill-details-inner {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.35s;
  }

  .skill-card:hover .skill-details,
  .skill-card:focus-within .skill-details,
  .skill-card.is-active .skill-details {
    max-width: 520px;
    opacity: 1;
    transform: translateX(0);
    flex: 1 1 auto;
    margin-left: 1.5rem;
    overflow: visible;
  }

  .skill-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 1rem;
    width: 100%;
    min-width: 0;
    align-content: center;
  }

  .skill-list-item {
    width: 100%;
    box-sizing: border-box;
    min-width: 0;
    display: grid;
    gap: 0.5rem;
    padding-bottom: 0.8rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .skill-name {
    display: inline-flex;
    align-items: center;
    width: fit-content;
    max-width: 100%;
    font-weight: 600;
    color: var(--text);
    font-size: 0.92rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding: 0.3rem 0.7rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 180, 80, 0.4);
    background: rgba(255, 180, 80, 0.1);
    opacity: 0;
    transform: translateX(-10px);
    transition:
      opacity 0.25s ease,
      transform 0.25s ease;
  }

  .skill-card:hover .skill-name,
  .skill-card:focus-within .skill-name,
  .skill-card.is-active .skill-name {
    opacity: 1;
    transform: translateX(0);
    transition-delay: 0.18s;
  }

  .skill-description {
    color: var(--text-muted);
    font-size: 0.92rem;
    line-height: 1.5;
    opacity: 0;
    transform: translateX(-10px);
    transition:
      opacity 0.25s ease,
      transform 0.25s ease;
  }

  .skill-card:hover .skill-description,
  .skill-card:focus-within .skill-description,
  .skill-card.is-active .skill-description {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.35s;
  }

  .philosophy {
    --philosophy-ease: cubic-bezier(0.23, 1, 0.32, 1);
    background:
      linear-gradient(
        140deg,
        color-mix(in srgb, var(--primary) 10%, transparent),
        transparent 55%
      ),
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--bg-light) 35%, transparent),
        transparent 65%
      ),
      var(--bg);
    padding: 3rem;
    border-radius: 20px;
    border: 1px solid color-mix(in srgb, var(--primary) 35%, var(--border));
    box-shadow:
      var(--shadow-card),
      0 0 0 1px color-mix(in srgb, var(--primary) 18%, transparent);
    text-align: center;
    position: relative;
    overflow: hidden;
    transition:
      transform 220ms var(--philosophy-ease),
      box-shadow 220ms var(--philosophy-ease),
      border-color 220ms var(--philosophy-ease);
  }

  .philosophy::before {
    content: '';
    position: absolute;
    width: 260px;
    height: 260px;
    top: -120px;
    right: -80px;
    background: radial-gradient(
      circle,
      color-mix(in srgb, var(--primary) 22%, transparent),
      transparent 65%
    );
    opacity: 0.8;
    pointer-events: none;
  }

  .philosophy::after {
    content: '';
    position: absolute;
    inset: 12px;
    border-radius: 14px;
    border: 1px solid color-mix(in srgb, var(--primary) 22%, transparent);
    opacity: 0.6;
    pointer-events: none;
  }

  .philosophy:hover,
  .philosophy:focus-within {
    transform: translateY(-4px);
    box-shadow:
      var(--shadow-card),
      0 0 22px color-mix(in srgb, var(--primary) 16%, transparent);
    border-color: color-mix(in srgb, var(--primary) 55%, var(--border));
  }

  .philosophy-content {
    position: relative;
    z-index: 1;
    display: grid;
    gap: 1rem;
    justify-items: center;
  }

  .philosophy h3 {
    font-size: 1.1rem;
    color: var(--primary);
    margin: 0;
    font-family: var(--font-display);
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .philosophy p {
    font-size: 1.1rem;
    color: var(--text);
    font-style: italic;
    line-height: 1.6;
    margin: 0;
    max-width: 620px;
    margin: 0 auto;
  }

  @media (max-width: 1100px) {
    .skills-disclosure {
      flex-direction: column;
      gap: 1.25rem;
      align-items: stretch;
    }

    .skill-card {
      flex: 1 1 100%;
      height: auto;
      width: 100%;
      max-width: 100%;
      min-width: 0;
    }

    .skill-card:hover,
    .skill-card:focus-within,
    .skill-card.is-active {
      flex: 1 1 100%;
    }

    .skill-card article {
      flex-direction: column;
    }

    .skill-details {
      width: 100%;
      max-width: none;
      display: grid;
      grid-template-rows: 0fr;
      opacity: 0;
      transform: translateY(-6px);
      margin-left: 0;
      transition:
        grid-template-rows 0.45s ease,
        opacity 0.35s ease,
        transform 0.35s ease;
    }

    .skill-card:hover .skill-details,
    .skill-card:focus-within .skill-details,
    .skill-card.is-active .skill-details {
      grid-template-rows: 1fr;
      opacity: 1;
      transform: translateY(0);
      margin-left: 0;
      overflow: visible;
      margin-top: 1.25rem;
    }

    .skill-details-inner {
      overflow: hidden;
    }

    .skill-card:hover .skill-details-inner,
    .skill-card:focus-within .skill-details-inner,
    .skill-card.is-active .skill-details-inner {
      transition-delay: 0s;
    }

    .skill-card:hover .skill-description,
    .skill-card:focus-within .skill-description,
    .skill-card.is-active .skill-description {
      transition-delay: 0s;
    }

    .skill-card:hover .skill-name,
    .skill-card:focus-within .skill-name,
    .skill-card.is-active .skill-name {
      transition-delay: 0s;
    }

    .skill-title {
      font-size: 1.4rem;
      writing-mode: horizontal-tb;
      height: auto;
      letter-spacing: 0.02em;
      width: 100%;
    }
  }

  @media (max-width: 768px) {
    .skills {
      padding: 3rem 1rem;
    }

    .philosophy {
      padding: 2rem 1.5rem;
    }

    .skill-card {
      padding: 1.5rem;
      height: auto;
      min-height: 0;
      width: 100%;
      max-width: 100%;
      min-width: 0;
    }

    .skill-card article {
      flex-direction: column;
    }

    .skill-details {
      margin-left: 0;
    }

    .skill-card-header {
      align-items: flex-start;
    }

    .skill-title {
      font-size: 1.4rem;
      writing-mode: horizontal-tb;
      height: auto;
      letter-spacing: 0.02em;
      width: 100%;
    }
  }
</style>
